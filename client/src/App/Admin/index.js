import React from 'react';
import {Button} from '@mui/material';

const index = () => {
  return (
    <div>
      <h1>Admin Panel</h1>
      <nav>
        <Button>
          <li>Logout</li>
        </Button>
      </nav>
    </div>
  )
}

export default index